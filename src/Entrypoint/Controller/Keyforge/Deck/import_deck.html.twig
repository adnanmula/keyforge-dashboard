{% extends "Keyforge/Shared/base.html.twig" %}

{% block title %}Import{% endblock %}

{% block body %}
    {% if result != false %}
        <div class="alert alert-{% if success == true %}success{% else %}danger{% endif %}" role="alert">
            {{ result }}
        </div>
    {% endif %}

    <div class="divider py-1"></div>

    <form action="{{ path('keyforge_deck_import') }}" method="post">
        <div class="row">
            <div class="col-1 col-sm-4 col-lg-3"></div>
            <div class="col-12 col-sm-12 col-lg-6">
                <input type="text" class="form-control" id="deck" name="deck" placeholder="Deck id">
                <div class="divider py-2"></div>
                <button type="submit" class="col-4 btn btn-block btn-dark w-100">Import</button>
            </div>
        </div>
    </form>
{% endblock %}